# To-Do List App

A minimal, client-side productivity app built with vanilla HTML, CSS, and JavaScript. No frameworks, no build step — just open `index.html` in a browser.

## Features

-Time-based greeting — Good Morning / Afternoon / Evening based on your local time
-Live clock — digital clock that updates every second with the current date
-Focus timer — start, stop, reset, and customize countdown duration with a clean display
-Task list — add, edit, complete, and delete tasks; persisted in Local Storage
-Quick links — save favorite URLs as one-click pill buttons; persisted in Local Storage
-Light / Dark mode — toggle between minimal light and dark themes; choice saved in Local Storage

## Usage

1. Open `index.html` directly in a browser, or serve the folder with any static file server.
2. Add tasks in the To-Do section, check them off when done.
3. Start the focus timer to begin a Pomodoro session.
4. Save your favorite links in the Quick Links section for fast access.

All data is saved automatically in the browser's Local Storage — no account or internet connection needed.

## File Structure

```
MiniProjectToDoList/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
└── README.md
```

## Browser Support

Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
